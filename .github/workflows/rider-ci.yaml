name: rider-ci
run-name: rider-ci workflow in github actions

on:
  push:
    branches: [main]
    paths:
      - "rider/**"
  pull_request:
    branches: [main]
    paths:
      - "rider/**"

jobs:
  build-and-push-rider:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Login to GHCR 
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{github.repository_owner}}
          password: ${{secrets.CR_PAT}}
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push rider image
        uses: docker/build-push-action@v5
        with:
          context: rider
          file: rider/Dockerfile
          push: true
          tags: |
            ghcr.io/${{github.repository_owner}}/food_rider:${{github.sha}}


