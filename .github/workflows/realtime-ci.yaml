name: realtime-ci
run-name: realtime-ci workflow in github actions

on:
  push:
    branches: [main]
    paths:
      - "realtime/**"
  pull_request:
    branches: [main]
    paths:
      - "realtime/**"

jobs:
  build-and-push-realtime:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Login to GHCR 
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{github.repository_owner}}
          password: ${{secrets.CR_PAT}}
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push realtime image
        uses: docker/build-push-action@v5
        with:
          context: realtime
          file: realtime/Dockerfile
          push: true
          tags: |
            ghcr.io/${{github.repository_owner}}/food_realtime:${{github.sha}}


